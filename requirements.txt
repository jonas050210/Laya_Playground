# start.py installs these automatically on first run.
# Manual install:  pip install -r requirements.txt
#
# For a CPU-only torch (much smaller download), install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

torch
transformers>=4.45
safetensors>=0.4
huggingface_hub>=0.20
numpy>=1.20
laya>=0.3.2
fastapi
uvicorn
